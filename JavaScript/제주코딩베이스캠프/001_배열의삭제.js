/**
다음 배열에서 400, 500를 삭제하는 code를 입력하세요.
var nums = [100, 200, 300, 400, 500];
 */
var nums = [100, 200, 300, 400, 500];

// nums.splice(3);			// 기존배열을 조작
// nums.pop(); nums.pop();
// nums = nums.slice(0, 3);	// 생성된 배열을 반환

console.log(nums);
